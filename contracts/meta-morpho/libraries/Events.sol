// SPDX-License-Identifier: GPL-2.0-or-later
pragma solidity ^0.8.0;

library Events {
    event RiskManagerSet(address indexed riskManager);

    event AllocationManagerSet(address indexed allocationManager);
}
