.
└── constructor(address pool, uint32 delay)
    ├── COLLATERAL_FEED = (UNISWAP_V3, pool)
    ├── COLLATERAL_SCALE = 1e18
    ├── BORROWABLE_SCALE is uninitialized
    └── PRICE_SCALE is uninitialized

.
└── collateralPrice()
    └── return pool.price(delay) from lib
