.
└── constructor(uint256 scaleFactor, address collateralPool, address borrowablePool, uint32 collateralPriceDelay, uint32 borrowablePriceDelay)
    ├── PRICE_SCALE = priceScale
    ├── COLLATERAL_FEED = (UNISWAP_V3, collateralPool)
    ├── COLLATERAL_SCALE = 1e18
    ├── BORROWABLE_FEED = (UNISWAP_V3, borrowablePool)
    └── BORROWABLE_SCALE = 1e18

.
└── collateralPrice()
    └── return collateralPool.price(collateralPriceDelay)

.
└── borrowablePrice()
    └── return borrowablePool.price(borrowablePriceDelay)

.
└── price()
    └── return scaleFactor * collateralPool.price(collateralPriceDelay) / borrowablePool.price(borrowablePriceDelay)
