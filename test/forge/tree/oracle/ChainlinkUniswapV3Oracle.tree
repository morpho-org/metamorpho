.
└── constructor(uint256 scaleFactor, address feed, address pool, uint32 borrowablePriceWindow, address borrowablePriceQuoteToken)
    ├── BaseOracle(scaleFactor)
    ├── ChainlinkCollateralAdapter(feed)
    └── UniswapV3BorrowableAdapter(pool, borrowablePriceWindow, borrowablePriceQuoteToken)

.
└── collateralPrice()
    └── return feed.price()

.
└── borrowablePrice()
    └── return pool.priceX128(borrowablePriceDelay, pool.token0() == borrowablePriceQuoteToken)

.
└── price()
    └── return scaleFactor * feed.price() * 2^128 / pool.priceX128(borrowablePriceDelay, pool.token0() == borrowablePriceQuoteToken) / 10 ** feed.decimals()
