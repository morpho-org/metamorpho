.
└── constructor(address feed)
    ├── BORROWABLE_FEED = (CHAINLINK_V3, feed)
    ├── BORROWABLE_SCALE = 10 ** feed.decimals()
    ├── COLLATERAL_SCALE is uninitialized
    └── SCALE_FACTOR is uninitialized

.
└── borrowablePrice()
    └── return feed.price() from lib
