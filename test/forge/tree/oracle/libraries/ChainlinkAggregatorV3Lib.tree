.
└── price(IChainlinkAggregatorV3 priceFeed)
    ├── if answer <= 0, revert with price is negative
    └── else return answer

.
└── price(IChainlinkAggregatorV3 priceFeed, IChainlinkAggregatorV3 sequencerUptimeFeed, uint256 gracePeriod)
    ├── if sequencerUptimeFeed answer == 0
    │   └── revert "sequencer is down"
    ├── if block.timestamp - sequencerUptimeFeed startedAt == 0 <= gracePeriod
    │   └── revert "grace period is not over"
    └── else return price(priceFeed)
